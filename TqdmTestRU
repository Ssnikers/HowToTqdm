#tqdm - Показывает ход выполнения действия в виде индикатора выполнения
#Давайте посмотрим код

import random
from time import sleep
from tqdm import tqdm

init()

print("Ваш текст")
for i in tqdm(range(100), colour="green"):
	sleep(random.uniform(0.01, 0.1))
  
 #Теперь вы увидели обычный код для tqdm
 #Теперь давайте разберемся по порядку
 #range - до скольких процентов будет отображаться индикатор выполнения
 #colour - цвет индикатора выполнения
 #sleep(random.uniform(0.01, 0.1)) - 0.01, 0.1 Время за которое индикатор выполнения заполниться
 #ЭтоВсё)
